<section>
  <div class="hero">
    <div class="container">
      <h1 class="hero__title">
        <span class="headline">
          {{ $data['title'] }}
        </span>
      </h1>
    </div>
    <img class="hero__bg" src="{{ $data['bg']['url'] }}" alt="{{ $data['title'] }}" />
  </div>
</section>